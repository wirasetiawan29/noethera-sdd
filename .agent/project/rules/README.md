# Project-specific rules

Add stack- or domain-specific rules here (e.g. `react-conventions.md`, `go-project-rules.md`).

These override or extend the global rules in `.agent/rules/`. Load after global rules.
